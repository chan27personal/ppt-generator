# 복사해서 .env 로 저장 후 값 수정:  cp .env.example .env
# DB 비밀번호 (강력하게)
POSTGRES_PASSWORD=change-this-to-a-strong-password
# 접속 도메인 (무료: DuckDNS 등). 예) myppt.duckdns.org
# 도메인 없이 IP로만 테스트하려면 Caddyfile의 {$DOMAIN}을 :80 으로 바꾸세요.
DOMAIN=your-subdomain.duckdns.org
